# Split: exact-provider offline worker integration

Tracking: #198; design gate: #199 and `docs/design/split-readonly-delegation.md`.

## Exact-host Chromium gate — 2026-09-19

### Current continuation: state-store dependency closure

The next normally admitted matrix (`wc_job_TxkVGRcfmYMc9e3q`) passed the baseline and alpha.1 but rejected rc.1 before page load on `zustand/vanilla`; rc.2 was not reached. This was an actual failed local run, not another platform denial. Integrity-verified inspection of the exact rc.1 store, slots, primitives and dockkit roots found that the store also imports `immer` and Zustand's middleware/shallow/vanilla entries without manifest dependency edges. The other shared roots were checked together rather than treating the first missing import as the complete dependency inventory.

The private-shell allowlist now contains **18** exact third-party runtime pins, adding `immer@10.2.0` and `zustand@4.4.7` from the frozen store dependency graph. Its regression verifies the union of store, slots and primitives runtime dependencies against each owning package's installed metadata; type-only packages and the separately pinned React pair remain excluded. Two additional negative controls reject unresolved Zustand and Immer imports. The selected DSH libraries remain exact-host packages, not baseline substitutions. Product dependencies, lockfile, public bundles and defaults are unchanged.

Final runtime-code checks in Workflow Session `wc_sess_KxNkj40jHGMmy4AN`: **54** focused cases plus typecheck passed; `pnpm run check` passed **113 files / 1,228 tests** (`wc_job_FY-GrC5E2rxRkZiA`). The preceding 38 UI regressions and baseline four-flow browser check passed before the two new shell pins, and are not relabeled as the new four-host result.

The final local browser matrix **passed** (`wc_job_QFD3sWUsobHRmy5t`, Node 22.22.3 / Chromium 153.0.8010.12): all four exact hosts completed their 58 worker/HTTP scenarios and four full browser paths, totaling **232 worker/HTTP executions plus 16 Chromium flows in four fresh host processes**. Each path created one parent and admitted one decision; rejection created no child, other paths created one child, cleanup was confirmed and reload recovered without another creation/decision. Every host reported zero external browser requests, zero fatal page errors and zero real provider dispatches. Baseline used 21 exact client packages/six shared seeds; newer hosts used 24/seven. All hosts imported private implementation digest `a48d6a6821c46c906420291fa7a13752f1558c4ce76f69d9c9afde2fa370e45e`.

The complete secret-free machine report is [split-browser-final-host-acceptance.json](split-browser-final-host-acceptance.json), SHA-256 `53c550a28facdeeb4eeebd2405d8bb0ede97a7117335a6f80818e0623becc19b`. It was reread and passed both the exact-host matrix validator and browser report contract. Its source is the preserved repair on `3615293` plus the two state-store pins and corresponding tests; the subsequent commit packages that same runtime code. Final-commit remote CI is still a separate required readback on #200. Earlier failed and interrupted runs below remain historical. This is an ephemeral, synthetic-provider composition, not user-product acceptance, persistence recovery, a normal installed profile or a live usefulness/cost comparison. No merge, release, deployment, real credentials or live model requests are included.

### Earlier continuation: initial shared-library dependency pins, matrix blocked

Workflow Session `wc_sess_ZktFvdLi3UWjlFgX` resumed the preserved local corrections on `3615293` without changing the branch or any service. The previously blocked baseline browser check now completed (`wc_job_uT4Itcnc6XPPLoD_`): four full flows passed with zero external browser requests and zero fatal page errors. The then-current source passed the full check (113 files / 1,225 tests, `wc_job_Na0txwSGkC855WwU`) and 38 browser regressions (`wc_job_0EbOVc52p6vPTLva`). These precede the additional vendor correction below and are not final-code full-check evidence.

The actual local four-host browser matrix (`wc_job_m_CiezrsON5OxnCN`) passed baseline and alpha.1, then failed rc.1 before page load with `Unresolved browser import: anser`; rc.2 was not reached. The new import guard exposed another missing dependency rather than masking the earlier `clsx` failure. Inspection of integrity-verified exact rc.1 primitives/dockkit packages found a group of runtime imports not declared in those package manifests, including Markdown, math and highlighting dependencies. This is an incomplete private-shell composition finding; it is not proof that a standard installed DSH profile has the same failure.

The local repair now supplies an explicit 16-package third-party allowlist, `SPLIT_BROWSER_VENDOR_DEPENDENCIES`, with exact versions already present in the frozen development graph. A new test checks every pin against actual installed dependency metadata and the baseline primitives runtime dependency set, excluding type-only packages. The selected exact-host DSH libraries are still used, never borrowed from the developer checkout. These pins apply only to disposable browser fixtures; no runtime package declaration, lockfile, public bundle, feature default or active profile is changed. Unknown imports still fail instead of causing automatic installation.

The final correction passed **52 focused tests plus both TypeScript checks** through `split-browser-shell-dependencies-regressions`. Its next four-host browser validation was blocked by platform safety review before a Job/execution receipt was returned. That operation and dependent commit/push stopped; no alternate test execution route was used. The uncommitted corrections remain in the original worktree. PR #200 remains at `3615293` with its existing failed browser check; no new remote CI or four-host acceptance is claimed. Next required work is the final-code complete check and exact-host browser matrix, followed by commit/push and exact-head CI only when validation can run normally. No merge, release, deployment or real-provider request is authorized by this checkpoint.

### Previous interrupted attempt (historical)

PR #200's remote head was `3615293475bcf47d6431de9698be5c933695b1ff`. CI `35445990393` completed with the new browser matrix failing at rc.1; its baseline and alpha.1 full browser paths passed, rc.2 was not reached. The ordinary Node checks, worker matrices and CodeQL do not turn that partial browser result into a pass. The subsequent `clsx` pin and unresolved-import guard were local, uncommitted corrections: 51 focused tests and typecheck passed. The next baseline browser validation call was blocked by platform safety review, so that operation and dependent validation/commit/push work stopped without an alternate execution route. No final four-host browser acceptance was claimed. Existing jobs were only observed to termination.

`check-split-matrix.mjs --browser` now runs the existing 58 worker/HTTP scenarios and the four complete Chromium conversation paths in each exact, disposable host process. The browser runner accepts that host's already-verified implementation/import resolver; it loads that host's published client bundles and aliases shared React/Cordis/store/slot/primitives imports to the same installation. Package versions, asset hashes and real paths are checked before browsing. Escaping symlinks, mixed frontend versions, worker-only evidence, missing flows, duplicate creates/decisions, failed reload recovery or unconfirmed child cleanup cannot pass the browser report contract.

Approve with a lost decision reply/reconnect, reject, running revoke, and lost creation reply/reload retain the existing real Gateway, Session Controller and Conversation/Chat assertions. Browser HTTP is limited to the disposable loopback origin, service workers are blocked, and uncaught page errors fail acceptance. Optional settings/workspace controller console warnings are not a claim of full profile acceptance. The workspace catalog and credentials remain ephemeral/synthetic, not a production profile, named-user ACL or persistent worker implementation.

Reproduce using an explicit test browser cache, also set by the CI browser job:

```sh
PLAYWRIGHT_BROWSERS_PATH=node_modules/.cache/playwright pnpm exec playwright install chromium
PLAYWRIGHT_BROWSERS_PATH=node_modules/.cache/playwright node scripts/check-split-matrix.mjs --browser
```

Local implementation checks passed 113 files / 1,217 tests, 38 Chromium regressions and the baseline's four full browser paths. Exact-host results are independently read back from the new matrix/CI; these local results do not certify unexecuted newer-host browser paths. Product source/bundles, defaults, package versions, dependency locks, published 4.37 and ports 3080/3081 are unchanged. No real model request is authorized or made. Human experience, real usefulness and persistent authorization/budget recovery remain separate gates.

The first submitted head (`2e316a4`) failed workflow initialization before any CI test job ran: job-level `env` used the unsupported `runner` expression context. The cache now uses a literal project-relative path, made absolute by the matrix before changing the child HOME/cwd, with a workflow regression assertion. The initialization failure is not a worker/browser test result and is not relabeled as passing.

The next remote run (`11ceb71`) passed the standalone browser baseline but found that the disposable npm host did not include `react-dom`, needed by the private browser shell. Browser-mode installation now explicitly pins the matching React/React DOM versions from the frozen development installation into each temporary host; no code is borrowed across runtime roots and product dependencies remain unchanged. The local expanded matrix attempt timed out during its first host's npm installation, before any browser scenario; it is infrastructure failure evidence, not an accepted host result.

At `e8f90ea`, the strict selected-root check then exposed the missing `dsh-client-store`: the original host closure did not enumerate browser-only seeds and `dsh.client.inject` edges. The shared exact-version resolver now has an opt-in browser closure, while its default runtime-only behavior is retained and tested. Browser fixture roots and bundle collection share one manifest, so missing dependencies are not fixed by falling back to a neighboring checkout or by downgrading the browser components.

The next run (`fc8f826`) completed the baseline's full browser acceptance and reached the newer alpha.1 UI, which requires the shared `dsh-client-ui-dockkit` library. Its exact alpha.1/rc.1/rc.2 packages have no `./client` plugin export. The private shell now bundles that selected-host library with the same React instance and seeds it before loading client plugins; it is not registered as a fake plugin or replaced with baseline UI. The old baseline does not acquire this newer library.

At `3615293`, baseline and alpha.1 browser flows passed; rc.1's published dockkit source imported `clsx` without declaring that dependency (rc.2 has the same shape). The disposable shell now pins the verified `clsx@2.1.1` utility along with its React pair. A syntax-tree check rejects any remaining literal bare import in generated browser chunks before page load; this catches the observed unresolved import without supplying remote scripts or borrowing packages from the developer runtime.

## Post-4.37 Conversation compatibility gate — 2026-09-19

The worker branch includes design #199 `cc979bc9b09300b533c76d5a81b774415d69a5b4` and exact published-documentation main `e5772cd8a5c47f30b5ab14fe73d2901914348463` through a normal merge (`e10e9b5`). Generated build conflicts were resolved by rebuilding the combined source, not by selecting one old artifact. The existing cleanup quarantine, connection-lifetime checks, authenticated approval and default-unregistered production boundary are retained. This does not republish or alter the immutable 4.37 release.

The previous 52-scenario exact-host matrix did not include the six Conversation cases, which only ran on the development baseline. A new contract test first reproduced that coverage gap. All six are now mandatory in each host run, and the report checks actual Gateway/Session Controller evidence plus their exact package versions. Worker-only reports, missing controller/Gateway evidence, mismatched controller versions and nonzero live dispatches are rejected; five negative report tests cover those cases.

The first expanded matrix passed all 58 scenarios on `0.1.2-rc.1`, then failed `conversation-allow` on `0.1.5-alpha.1`: the actual Session Controller was not active. Inspection of that exact published package identified its required `fileUploads` service. The disposable composition now initializes real Connection, Commands and FileUploads where required by the controller's declared injection contract. It does not mock that service, replace prompt admission, enable persistence, change the installed user profile, or treat uploads as an approved child capability. After this correction, all four hosts passed their 58 scenarios. The first failed run remains a failure, not an accepted result.

Final-code local verification on M15 / Node 22.22.3:

- `pnpm run check`: **112 files / 1,197 tests**, lint, types, build, CLI, compatibility and package checks passed (`wc_job_sF9FpOPaYk7Hl3Tx`).
- Chromium: **9 files / 38 tests** passed; authenticated lost-decision/reconnect/reload and actual Gateway/Conversation approve, reject, revoke and lost-creation-reply paths passed (`wc_job_y-szntp1SV38HbKT`).
- Exact-host matrix: **4 fresh host processes / 232 scenario executions**, including all six Conversation HTTP/controller paths on every host (`wc_job_efDWfKgI7sEs9c1_`). Each used private bundle digest `a48d6a6821c46c906420291fa7a13752f1558c4ce76f69d9c9afde2fa370e45e`. A bounded summary is in [the local evidence record](split-post437-host-acceptance.json).

All source fixtures, signing data, credentials and provider replies remain synthetic; real provider dispatches are zero. The four-host Conversation checks exercise actual local HTTP, Gateway composition and Session Controller, **not Chromium on every host**. The separate full browser checks still use the exact development baseline and an ephemeral workspace catalog. Real-profile UI compatibility, human acceptance, live usefulness and durable permissions/budgets remain open. Ports 3080/3081, their settings and the preview-only native-compaction host patch are not touched. Current-head remote CI is recorded separately on #200 after it completes; prior CI is not reused as proof.

## September 19 engineering refresh

The implementation is reconciled with design head `b0bbbbb31b5c04e785767b68b08bba5c59250cf0`, including main `085d1d618a01bba6f28ed002f7d5af088a85d4d0`. The existing approval, authenticated transport and experimental Conversation entry remain intact. The sections dated September 12 below are historical evidence, not current-head results.

The cleanup review reproduced an actual admission defect: after `run.dispose()` removed a child and then rejected, a fresh evidence grant on the same parent started a second child (expected one, observed two). The controller now requires both successful disposal and registry absence before releasing the parent slot or its closed-scope request guards. Cleanup failure leaves that parent's Split admission quarantined for the current experimental lifetime; it does not cancel ordinary parent tools, grant a retry, or expose a reset control. A new grant alone cannot erase the failure. Three additional unit cases cover failure before/after removal and pending disposal followed by a successful new grant. The existing cross-host `approval-disposal-failure` scenario now also checks re-admission: the matrix still has 52 scenarios, with a stronger assertion rather than a relabeled old result.

Baseline reconciliation regenerates `lib/` from the combined source and uses main's `tsdown` 0.23.0 with the matching development-only `@tsdown/css` 0.23.0 required by the experimental browser bundle. Shared direct lock resolutions are preserved apart from this CSS peer annotation. An image-view fixture's nested `call` now matches its actual `{ name, argsRaw }` host type; its top-level call identity and both PTC image regression cases remain intact. No runtime image behavior or Remember test standard is changed.

GitHub default CodeQL setup does not normally scan pull requests targeting an unprotected feature branch. The additional `CodeQL stacked PR` CI jobs analyze the exact head's JavaScript/TypeScript and Actions with read-only repository permissions and require valid successful SARIF reports with zero findings. Results are retained as language/SHA-named artifacts; `upload: never` and `upload-database: false` preserve the existing repository default setup rather than replacing its security-dashboard results. Missing/failed reports and findings fail the jobs. Once this stack targets main, ordinary default-setup analysis is again the merge gate. See [GitHub setup types](https://docs.github.com/en/code-security/concepts/code-scanning/setup-types) and the [pinned analyze action](https://github.com/github/codeql-action/blob/1c5b675653bb5c22dbe9b12b556ec555138e09fd/analyze/action.yml).

This refresh does not enable Split, change #216 or ports 3080/3081, or authorize live model use. Human product acceptance, persistent permissions/budget reconstruction and real delegation usefulness remain open. The separate Remember A/B controls are now accepted only within their documented limited scope; their success does not validate this worker or its persistent composition. Current-head local and remote validation is recorded on #200 after execution.

Local refreshed-code validation on M15 / Node 22.22.3: `pnpm run check` passed 110 files / 1,166 tests, including the stronger cleanup checks and seven report-validator cases. `pnpm run test:browser` passed 9 files / 34 tests. The authenticated browser gate passed with a deliberately lost decision reply, one decision, two child requests and reload recovery. The separate actual Gateway/Session Controller/Conversation gate passed its four allow/reject/revoke/lost-creation-reply scenarios. All provider replies and credentials were synthetic. The CodeQL language matrix and wording were finalized afterward; the final YAML/pack checks and exact-head remote analysis remain separate from these local tests. No previous matrix report is re-labeled as a fresh result.

The first remote supplemental Actions scan at `521dc76` executed successfully with 17 query rules and zero findings, but the new report checker incorrectly looked only in the empty driver rule table. The downloaded SARIF placed its actual query-pack rules under `tool.extensions`. The checker now validates rules across both standard locations, with an additional regression test; it still rejects missing rules, failed execution and any finding. This corrects report handling, not a security finding. That earlier overall CI is not counted as passing. CodeQL uses its default PR analysis behavior (including diff-informed analysis when selected by the action); it is not proof of absence of all vulnerabilities.

The same head's JavaScript analysis reported `js/shell-command-injection-from-environment` on the Split matrix's absolute test-script path reaching the shared command/script launcher. This was a static report, not a demonstrated exploit or a worker permission escape. Instead of suppressing it, the matrix now uses a dedicated `runBoundedNode` path with `process.execPath`, literal argv and `shell: false`; generic Windows command-shim handling is no longer on this Node path. Both entry points share the unchanged bounded-output/process-tree-cleanup lifecycle. New tests cover special-character script paths and arguments, output limits and timeouts, while the existing Windows canary additionally exercises literal Node argv. The result must be confirmed by the next exact-head scan, not inferred from this refactor.

After those CI-helper corrections, the complete local check passed again: **111 files / 1,170 tests**, plus lint, typecheck, build, CLI, compatibility and package checks. The corrected report checker also rejects the original JavaScript SARIF finding, proving it was not silently filtered out. Remote host/Node matrices and fresh CodeQL reports are still required on the subsequent exact commit.

At `c73095b`, supplemental Actions analysis passed, but JavaScript still reported the shared launcher's older call paths (including the ordinary installation matrix and native-compaction process checks). The actual execution boundary now separates native executable argv from the Windows-only, escaped command-script branch instead of merging both forms into one invocation object. The three direct Node callers for the ordinary matrix, CLI checks and installed native-compaction checks also use `runBoundedNode`. Windows shim escaping, generic timeout/cleanup behavior, test inputs and acceptance assertions remain unchanged. Both direct and generic native paths now have a literal special-character path/argv regression. This is engineering hardening prompted by a static report, not evidence that a live worker escaped its scope; no CodeQL rules or findings are suppressed.

The final separation passed the full local check: **111 files / 1,171 tests**, plus typecheck/lint/build and all CLI/package checks. Remote evidence remains specific to each head; the prior failed JavaScript scans are retained rather than treated as passes.

## September 19 conversation-admission closeout

The follow-up starts from `5ef7185cbf8656287555fe8d27a5fd2d2ec10ec8`, whose seven exact-head PR checks, including both supplemental CodeQL jobs, were read back as successful. The quarantine and command-launcher corrections above are retained; they are not newly implemented by this follow-up.

Four new client regressions first failed against that source: a throwing view subscriber interrupted creation after the UI had locked itself, and synchronous disconnect, connection replacement, or store disposal during staged-state notification still reached the injected create fetcher. These are deterministic client-boundary reproductions, not live-account incidents or evidence that server-side approval was bypassed.

The store now isolates subscriber exceptions, captures the originating connection's cancellation signal and epoch before notifying views, refuses already-stopped/disconnected creation, and checks cancellation before invoking the fetcher. The same four cases then passed. A reconnection does not automatically submit a replacement mutation, unlock an uncertain operation, change the allowed source manifest, or grant worker approval. Existing receipt, task-count, body-size and timeout limits remain unchanged.

Final-code local checks on M15 / Node 22.22.3 passed: `pnpm run check` ran **111 files / 1,175 tests** (`wc_job_axTkFFeSZ5G0ly4f`); Chromium ran **9 files / 34 tests** (`wc_job_4zpwUKuaSz3KzcH9`). The separate authenticated transport and actual Gateway/Session Controller/Conversation checks passed (`wc_job_BnfzP1RU2Zh6jKDE`): allow and lost-creation-reply recovery created exactly one child with two synthetic child requests; rejection created none; revocation stopped the one running child; every browser path recovered after reload. These remain synthetic-provider checks and did not access real credentials. Remote checks must be read from the new exact commit; the preceding head's successful scans do not substitute for them.

This changes only the internal experimental client, regression tests and documentation. Public `lib/`, dependencies, feature defaults, #216, ports 3080/3081 and the user's ongoing Remember experiment are unchanged. Hands-on product acceptance, real-provider task usefulness and persistent worker authorization/budget reconstruction remain separate gates. Do not deploy this ephemeral worker into the persistent 3081 profile merely because its offline checks pass.

## Status and delivery boundary

This is an internal, opt-in implementation prototype, not a deployed worker. The source controller is not imported by `src/index.ts`, registered in a standard profile, or exported as a public package entry. A separate development conversation entry now exposes it through real DSH browser components; see [experimental conversation acceptance](#experimental-conversation-acceptance). A small request-local bounds wrapper is integrated with the existing Codex adapter and is inert outside an explicitly entered Split scope. There is no new automatic delegation policy, shell tool, OAuth flow, or model fallback.

The development fixture installs the real `@deepseek-ai/dsh-subagent-spawn-in-process` and `@deepseek-ai/dsh-subagent-in-process-driver` at exactly `0.1.2-rc.1`. They are development dependencies only, not copied implementations or a new provider running inside Codex Connect. The worker uses the real DSH subagent registry, Agent factory/loop, tool runtime, structured-output capture and existing Codex Connect adapter. All provider replies, account data and source files used for these tests are synthetic.

## Implemented operation

A trusted host calls `snapshotSplitEvidence` with an explicitly approved root, exact relative file names and previously approved SHA-256 hashes. It receives a nominal in-memory capability, not a JSON permission token. The snapshot loader performs no directory scan and never discovers credentials. It refuses path escapes, symlinks, hardlinks, special files, unsupported source names, changed bytes, invalid UTF-8, null bytes and recognizable private-key/token content. Known-private-path and secret-pattern checks are defense in depth, not an exhaustive secret detector; the host-reviewed manifest and content identities are the approval boundary.

`attachApprovedSplitWorker` requires explicit enablement, an exact live top-level parent, that snapshot, a fixed brief and the exact trusted provider object. It registers one single-use parent tool, `inspect_with_worker`, with an empty argument schema. The model cannot supply a new task, path, model or budget. The grant captures the brief and provider function identity. Reuse and concurrent admission for the same parent are refused.

The real spawn provider starts one fresh child with depth one and no inherited conversation seed. Its creation-time `toolFilter: { allow: [] }` removes inherited tools. At synchronous `agent/created`, before model work, the controller installs native tool presentation, a snapshot-read tool, identity-checking execution guards and request/step limits. The official provider's own `structured_output` implementation is checked and retained. Unknown tools, reserved `run_code`, recursive delegation and same-name implementation replacement do not gain authority. The parent remains unaffected.

This is deliberately not a claim that a custom guard ran inside the upstream provider's unpublished setup callback. The initial inherited-tool restriction does; the additional guard is installed at the synchronous publication hook, before the exercised provider starts a model request. Trusted same-process plugins remain trusted, and this is not an operating-system sandbox. A fresh child still joins its parent's deployment/preset composition, but the consent follow-up now installs a child-local complete system prompt and suppresses runtime context through the host APIs. The request boundary rejects unexpected messages instead of silently stripping them. This restricts model-visible context, not the powers of trusted same-process plugins.

## Bounds and results

- At most 16 explicitly named files, 32,000 UTF-8 bytes each and 128,000 bytes in total. Reads use detached memory after snapshot completion; at most 24 reads, 16,000 bytes per read and 128,000 returned bytes.
- The approved brief is at most 16,000 bytes. One child per parent, depth one, at most six steps and six provider payload reservations; overrides may only reduce these limits.
- Requests must use ordinary `gpt-5.6-luna`, low effort, configured per-response output limit 2,048, SSE and zero provider retries. Reserve, different models/providers, priority tier, auxiliary compaction/title calls and non-function hosted tools are refused. A provider failure cancels the owned operation instead of triggering another route.
- The default 90-second deadline is cooperative. Result disposal is awaited, not abandoned by a Promise.race. A pending or failed disposal retains the parent admission slot and denial hooks even after child registry removal; a disposal failure cannot return completed success. This does not hard-kill arbitrary trusted in-process code.
- Structured results are limited to 16,000 bytes, ten findings and bounded explanations/references. Every reference must match a range the child actually read, including its content hash. Even an empty findings array requires an actual evidence read. The result is ordinary tool data, never a command or permission grant.

The installed DSH JSON-schema subset does **not** support `maxLength`, `maxItems` or `minimum`. The first integration run correctly rejected a schema containing those keywords. The corrected schema uses supported shape constraints, while code enforces numeric, array and UTF-8 byte bounds before the capture tool body and again before returning the result. Removing unsupported schema annotations did not remove the limits.

`requestReservations` counts payloads admitted by the plugin before provider dispatch. The fixture separately counts mock fetch calls and proves zero retry on a synthetic HTTP 500. Reservations are conservative: a later local failure can consume a reservation without a network dispatch. They are not a token invoice, backend usage figure or verified cost saving. The per-response output setting is not an aggregate token-cost cap. Operation elapsed time includes awaited disposal.

## Reproducible evidence

```sh
pnpm exec vitest run tests/split-evidence.spec.ts tests/split-dispatch.spec.ts tests/split-worker.spec.ts tests/split-readonly-boundary.spec.ts
pnpm run check
pnpm run test:browser
node scripts/check-split-matrix.mjs
```

The matrix checker resolves pi-ai under Node's ESM `import` condition from the isolated experiment and adapter locations, not `require.resolve`. pi-ai's root and package manifest are not CommonJS exports. The first actual isolated run exposed this checker defect before any scenario ran; that failed attempt is not acceptance evidence. The corrected fresh checker process uses `--experimental-import-meta-resolve`, verifies both resolutions are identical and inside the temporary host, and reads the package manifest as local metadata. Four subprocess regressions cover import-only exports, same-version duplicate providers, an out-of-root symlink and wrong package identity. This test-only Node flag does not change the production runtime.

The integration test drives an actual parent turn through a model-issued delegation call, real DSH spawn, model-issued approved snapshot read, official structured-result capture, parent tool-result consumption and a new parent completion. It checks that parent-only text is absent from child requests, child tools are limited, lifecycle events pair, the child disappears after disposal and the parent remains live.

Negative tests cover invalid paths/hashes/files, byte/read budgets, pre-cancellation, publication cancellation, active-parent disposal, cooperative timeout, repeated requests, overlapping admission, single-use grants, missing/invalid structured results, unread/fabricated evidence, excessive findings/output, recursive/reserved tools, implementation substitution, observer-driven route changes and asynchronous-observer cancellation. No live credentials are inspected and no real provider request is issued.

## Historical baseline source regression — 2026-09-12, before consent follow-up

On M15 / Node 22.22.3, the complete `pnpm run check` passed 100 test files / 974 tests, including typecheck, lint, build and package checks. Chromium regression passed eight files / 28 tests. These totals include the four ESM-resolution regressions and the stronger post-seal replacement assertions. The production `lib/` tree remained byte-for-byte unchanged from the preceding worker commit. Matrix execution is a separate gate; neither these counts nor the old Remember installation matrix substitute for its exact-host results.

## Historical baseline exact-host matrix — 2026-09-12, before consent follow-up

The corrected M15 matrix passed all four hosts on Node 22.22.3: DSH `0.1.2-rc.1` with pi-ai `0.84.4`, and `0.1.5-alpha.1`, `0.1.5-rc.1`, `0.1.5-rc.2` with pi-ai `0.85.1`. The checker verified respectively 214, 229, 231 and 231 exact-version DSH packages. Each host executed all 23 scenarios in its own fresh process: **92 scenario executions / four processes**, not 92 child processes. All hosts used internal bundle SHA-256 `3f15465a68eb39455eb6c206253f9c8ac1b43e6a2e001c49d122f082b605977b`.

The full non-sensitive report is `docs/experiments/split-host-matrix-m15-2026-09-12.json`. Every scenario passed, all exercised forbidden effects remained zero, and the controller remained inaccessible through the production entry. Real provider dispatches were zero. This establishes the internal synthetic Split integration across the exact hosts; it is not a published-package, real-provider, persistent-profile or restart/resume acceptance result. GitHub exact-head CI results are tracked on #200 separately rather than inferred from this local run.

## Deployment-composition review

The initial controller cannot reconstruct a process-local evidence grant, execution guard or budget after cold resume. It now rejects a mounted `sessionPersistence` service both when attaching a grant and when executing it, and rechecks at child publication. The fixture tests a real JSONL backend present at attachment and introduced after grant attachment. Both paths reject before a child request. This is a supported-composition gate, not a security sandbox against arbitrary trusted plugins changing the host during execution.

The additional tool identity seal runs at `agent/created`. A mutation before that point is part of trusted host composition, not an attack this seal authenticates. The older read-replacement test used that same event and could fail because the worker read tool did not exist yet. It now replaces the installed capability at `agent/session-start`, verifies the tool exists and that the child actually issues its tool call, and checks that the substituted body never executes. The matrix also checks post-seal structured-capture substitution and child-local tool additions. No claim is made that shape checking proves the original identity of an already-replaced trusted capture implementation.

The consent follow-up seals the model-visible system prompt with the host's `complete` section contract and suppresses child runtime-context snapshots. A fixture puts deployment-only text into both a global system section and runtime context: the parent retains it, the child request does not. Plugin-injected conversation context is rejected before adapter dispatch. The request validator allows exactly the fixed initial brief/catalog followed by model messages and tool results. Conflicting complete sections or unsupported composition fail closed. This does not authenticate or sandbox arbitrary same-process plugins, and custom profile/preset and persistent-profile acceptance remain separate gates.

## Explicit approval and revocation follow-up

`attachSplitApprovalRequest` stages a fixed, immutable review: workspace display label, task brief, exact source paths/SHA-256/line counts, ordinary Luna/low route, request/deadline limits, and the exact isolated system prompt. A fresh offer ID plus review digest correlates a decision; neither is a credential or a bearer authorization token. A model cannot call the decision/revocation methods. The low-level `attachApprovedSplitWorker` remains an explicitly host-approved internal fixture API; the consent entry never calls it with an omitted admission hook.

A model-issued parent tool call now waits for the real DSH `ApprovalService.request` inside the parent's open turn. Its `approval/asked` and `approval/decided` events form the normal host audit pair. This bridge only claims its own exact request object, and success requires both the service's `allowed-once` and the matching explicit decision received through this offer's controller. Missing service, `never` policy, unavailable/intercepting answerer, rejection, timeout, stale ID/digest, duplicate decision, or cancellation creates no child. An unrelated answerer's allowed-once is not promoted into a user decision. The service audit records that answerer's outcome faithfully even when our additional receipt check denies the operation.

The review reason is user data on this in-memory session/approval surface, not generic telemetry. Snapshots were locally prepared under the trusted host's explicit file/hash approval; consent here authorizes their use by the child provider. The 90-second cooperative budget currently includes waiting for approval and executing the child, and excludes no hidden retries. It is not an invoice or an aggregate token guarantee.

The worker handle's synchronous call now withdraws future use **and** aborts any owned work. `revoke()` additionally awaits operation settlement and verifies cleanup. Pending approvals are cancelled, late allow decisions cannot resurrect them, and running requests receive the abort signal. The UI stays `revoking` until quiescence; if disposal fails, even after registry removal, it reports failure rather than clean revocation. Parent disposal also withdraws the offer. Revocation cannot undo data already sent or tokens already consumed. Repeated revocations share one promise and never launch replacement work.

`src/client/SplitApprovalCard.tsx` is an isolated English/Chinese view with explicit Approve once / Reject / Revoke and stop controls. It renders task text as text, displays file identities and limits, disables duplicate decisions, separates unknown transport outcomes from confirmed decisions, and does not claim stopped until the authoritative view says so. Six Chromium tests exercise these controls, narrow-screen layout, inert HTML-looking text, and replacement-offer isolation. Those tests use mocked callbacks, not a deployed RPC or a browser-to-real-host end-to-end connection.

Seventeen additional shared host scenarios extend the previous 23 to 40: explicit allow/reject, absent/unavailable/never/intercepted approval, stale/double decisions and frozen review, revocation before/start/during work, owner disposal, approval timeout, post-approval provider change, disposal failure, isolated deployment prompt and rejected injected context. All use synthetic provider replies and accounts. Prior saved matrix JSON remains historical 23-scenario evidence; exact-head CI must freshly validate all 40 before treating this follow-up as cross-host accepted.

The consent-only stage introduced no HTTP route or client registration. The subsequent authenticated-transport stage below adds an internal route and connected view, still not registered by the public plugin. Everyday profile composition and persistence remain separate gates.

## System-prompt representation compatibility correction

The consent commit `15609d665b00b35e6f26bbf1e31fbcd3800418b5` passed local baseline tests but failed exact-head CI `34676946640` on DSH `0.1.5-alpha.1` in both Node jobs. The remaining rc hosts were not reached. This failure was not a provider refusal or an accepted cross-host consent result.

Inspection of the published exact host code confirmed that baseline DSH uses `options.system`, whereas the newer loop commits a leading `system/message` created with source `{ kind: 'plugin', plugin: '@deepseek-ai/dsh-system-prompt' }`. The old field-only guard rejected this legitimate representation. The guard has now been isolated into `src/split-context.ts` and accepts exactly one of these forms without modifying or deleting any request data. History form requires the exact isolated prompt, one text block, the checked host source and index zero, with no simultaneous system field. The fixed task must be the next user-role, user-source message; subsequent messages remain model responses or tool results only. Duplicate/later system messages, extra user/plugin context, wrong source/form/text and changed task content are rejected. These checks operate within the trusted host-composition boundary, not as authentication against arbitrary in-process code.

Before the correction, the extracted unchanged predicate failed three of 24 new tests: two valid history-form cases and a previously unchecked task-role substitution. After the correction, 27 context tests pass, including both representations and continuations, immutable request preservation, order-independent text properties and malformed/extra-context rejection. The real shared host fixture additionally records the prompt representation observed at `llm/stream` rather than inferring it from the test name. The complete current-head host matrix and CI remain required; earlier saved 23-scenario evidence is not reused for this correction.

## Authenticated browser transport follow-up

`registerSplitApprovalTransport` supplies an opt-in internal exact POST route at `/api/codex-connect/split-approval`. It reuses the installed DSH Connection's public `requestRejection` for Host/Origin and signed-browser-cookie authentication. The generic DSH RPC callback does not expose an authenticated principal or request headers, so this implementation uses a WebServer route with that same verifier, not a parallel login, OAuth flow, cookie decoder, or signature implementation. It additionally requires JSON, a custom same-origin header, a 4,096-byte body limit and a five-second body deadline. Rejections contain only a stable code and are not cached. Unread oversized bodies preserve the response long enough to send 413, then close the connection.

A trusted host interaction must explicitly bind the real nominal approval handle and exact live parent Agent to its authenticated request. The approval route has no wire-side bind/create/list action; the separate experimental conversation route below owns fresh creation. Neither a session ID, offer ID nor client-claimed user ID is an authority. The bound owner is a process-salted HMAC of the authority and the one cookie segment which the existing DSH verifier independently accepts. Raw cookies, signing secrets and owner fingerprints are never response fields or log data. Unrelated cookies do not change ownership; duplicate or ambiguous credentials are refused. Authentication, live parent object identity and owner binding are checked for every read and mutation, and again after body collection. Cordis service proxies are compared through their exported underlying-identity symbol, not transient proxy equality.

**Identity boundary:** DSH's inspected browser-auth contract is an authority-bound signed credential, not a named-user identity/ACL. Credential-equivalent tabs and copied browser credentials represent the same authenticated principal; separately minted credentials cannot control another binding. This layer does not introduce multi-user isolation across the rest of DSH, defend against trusted same-process plugins, or treat arbitrary client names as authenticated users. A re-login or host restart requires a new host-reviewed binding; no silent adoption occurs.

A per-transport process epoch plus offer/digest/revision checks rejects stale targets. At most one decision receipt and one revocation receipt are retained per offer, across at most 32 host-created bindings. Mutation admission reserves its receipt before notifying any observers. An exact duplicate operation returns status without invoking the decision twice; changed operation identities or conflicting choices are rejected. The decision receipt acknowledges recording the user choice, not successful child execution. Revocation acknowledgement remains pending until the underlying controller verifies cleanup; loss of an HTTP reply does not undo or repeat an admitted action. A stale revision cannot authorize a decision, while an authorized revocation of the same immutable offer may use an older revision so state progress does not prevent stopping it.

`SplitApprovalRemoteStore` uses same-origin Fetch credentials and the existing Connection generation source. Generation loss clears displayed review data, cancels local reads and prevents late responses from overwriting a new generation. Reconnect and explicit refresh only read status. Once a decision or revocation has been sent, this client never automatically resends it, even after an unknown outcome. Correlations, operation receipts and response size/shape are checked; earlier revisions cannot overwrite newer observations. `SplitApprovalRemoteCard` connects the existing card to this store, disables stale decisions, and leaves stopping distinct from confirmed cleanup. Status polling is bounded per request and stops at terminal/unavailable states or disposal. It is not a persistent permission-recovery mechanism.

The shared exact-host fixture adds 12 real HTTP/authentication scenarios (52 total): allow/reject, missing/invalid browser authentication, cross-origin/Host refusal, another authenticated credential, other/stale sessions/offers/epochs, nominal handle binding, operation replay/conflicts, revision races, bounded bodies, owner disposal and revocation completion. Signing data and model credentials are generated solely in isolated in-memory/test homes. These are local HTTP requests, not real provider dispatches.

`node scripts/check-split-browser.mjs` separately serves a private test build on an OS-assigned loopback port and uses actual Chromium, the unmodified published DSH Connection browser bundle, real DSH signed-cookie verification, the real approval controller and the real DSH child lifecycle. It sends the actual approval request, deliberately drops its HTTP reply, reconnects and reloads, then requires completed status with exactly one decision dispatch and two synthetic child requests. Only module-loader bootstrapping and Gateway generation-readiness signals are fixture adapters; this is not acceptance of the complete DSH Gateway or everyday UI profile. Browser auth cookies are never read from a real account or printed. All temporary routes, browser contexts, processes and directories are disposed.

Reproduce with `pnpm run check`, `pnpm run test:browser`, `node scripts/check-split-browser.mjs` and `node scripts/check-split-matrix.mjs`. The two CI Node jobs run the 52-scenario matrix; the browser job also runs the authenticated end-to-end gate. Historical JSON reports retain their original scope and are not relabeled. Current-head CI results are recorded on #200 after completion.

## Still required before product enablement

The new, separate `check-split-matrix.mjs` gate builds a private experiment artifact with every npm dependency external, then installs each declared exact DSH dependency closure into a disposable directory. Each host imports the same artifact bytes and its own actual AgentLoop, ToolRuntime, SubagentRuntime, spawn provider and in-process driver. It verifies all pinned DSH package versions and the adapter/provider pi-ai resolution identity before exercising the current `SPLIT_HOST_SCENARIOS` list (52 scenarios after the authenticated-transport follow-up). One fresh process per host runs the scenarios; this is not a worker restart/resume test. The experiment artifact is not the public npm package. Ordinary `check:dsh-matrix` remains a distinct shipped-functionality/Remember regression gate.

Both Node validation jobs run the Split gate independently. A missing host/scenario, failed scenario, changed artifact, reused host process, mixed runtime or enabled production entry rejects the report. Passing the internal matrix does not expose a package entry, install a profile, or approve production deployment composition. The experimental conversation gate below uses the exact development host, not the four-host matrix. Public profile registration and production composition remain separate acceptance.

## Experimental conversation acceptance

`registerSplitConversation(ctx, config)` explicitly installs fresh task admission at `/api/codex-connect/split-conversation`. The host supplies `enabled: true`, an absolute root, display label, immutable path/hash manifest, exact provider name and reduced worker limits. The browser supplies only a brief (at most 2,000 characters within the 4,096-byte HTTP body), process epoch and operation ID. It cannot select files, models, existing sessions, offers, owners or budgets. This entry refuses persistence and is absent from the shipped host/client entries and public exports.

Creation reserves a credential-bound receipt before awaiting any work, snapshots the host manifest, creates a fresh parent through the real Session Controller, attaches consent, and binds the exact authenticated request before admitting the ordinary logged prompt. A request guard prevents generic RPC from racing a model request ahead of admission. The parent may invoke the single inspection tool or answer without it; the entry adds no orchestration prompt. A source or prompt admission failure becomes a terminal failed receipt with no reusable approval target. A failed published Session remains quarantined by the request guard until experiment disposal; it is not silently adopted or retried. There is one task per credential and at most 32 receipts per process, including failures. Starting another experiment requires a new disposable process, not a permission reset in the browser.

The conversation route uses the same DSH trust verifier as approval, extracted into `split-http.ts`. Every creation retry with the same operation/brief returns its receipt; conflicting operations fail closed. Status lists only the requesting credential's receipt. Neither creation nor approval mutations are automatically retried. The browser recovers with bounded status-only reads, refreshes the real Session list before selecting a recovered parent, and renders the existing approval card only in that Session's `conversation.input.dock`. Gateway reconnects and page reloads do not grant new authority. Process restart loses all grants; no permission or child persistence is claimed.

The exact `0.1.2-rc.1` development composition uses real Gateway event generation, Session Controller, Connection/BrowserAuth, published Conversation and Chat renderers, and the real child lifecycle. The stock durable Workspace Registry requires session persistence, so this experiment deliberately supplies an empty ephemeral workspace catalog with all mutations refused. Parent Sessions have the configured cwd and appear ungrouped. The development entry loads published client bundles with shared Cordis/React modules; it is not the normal installed Web profile. Settings, credentials management, workspace management and command UI are not acceptance targets; absent optional host controllers can report unavailable. No real account is read, and every provider response/source is synthetic. The manual runner verifies control plumbing, not whether a real model chooses useful delegation.

Run automated acceptance with:

```sh
pnpm exec vitest run tests/split-conversation.spec.ts tests/split-conversation.client.spec.tsx
node scripts/check-split-conversation.mjs
```

The browser gate covers approve, reject, running revoke, a processed approval whose reply is dropped, and a processed creation whose reply is dropped. It checks one creation/decision, actual child counts and cleanup, parent-result text in the real Chat view, and full reload recovery. It runs separately from the existing transport-only browser gate and four-host worker matrix; those historical results retain their original scope.

For hands-on offline acceptance, run one of the following from this development checkout after installing Chromium with `pnpm exec playwright install chromium`:

```sh
node scripts/check-split-conversation.mjs --manual=allow
node scripts/check-split-conversation.mjs --manual=reject
node scripts/check-split-conversation.mjs --manual=revoke
```

Each command opens a fresh isolated browser already authenticated with disposable synthetic signing data. Enter an inspection brief, start the task, review the exact card, then approve or reject. For revoke, approve first and stop the running worker. Refresh to inspect recovery, then close the experiment tab to run the host assertions and clean up. The revoke fixture intentionally keeps its synthetic child request pending; its 60-second approval/execution deadline still applies. These commands do not read real Codex credentials, publish, deploy, or restart an existing service. Human completion of these manual paths remains distinct from automated Chromium evidence and from real-provider quality/cost comparisons.

There is no worker persistence/restart recovery, continuable child, automatic relaunch, multi-worker scheduling or live model comparison here. The controller must not be enabled in a production-like persistent profile before resume/permission reconstruction is defined and tested. No claim is made about usefulness, reasoning quality, token savings or real-backend worker behavior. Remember's limited real-provider A/B persistence controls and remaining long-task/automatic acceptance are tracked separately in #196/#65. No release, deployment, service restart or changes to ports 3080/3081 are part of this work.
